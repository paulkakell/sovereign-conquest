# Optional: override values in docker-compose.yml by copying to .env and editing.
# The compose file uses defaults that are suitable for local development.

# JWT_SECRET=change-me
# ADMIN_SECRET=change-me-to-enable-admin-endpoints
# INITIAL_ADMIN_USERNAME=admin
# INITIAL_ADMIN_PASSWORD=ChangeMeNow!
# UNIVERSE_SEED=2002
# UNIVERSE_SECTORS=200
# TURN_REGEN_SECONDS=120
# PORT_TICK_SECONDS=60
# PLANET_TICK_SECONDS=60
# EVENT_TICK_SECONDS=60  # set to 0 to disable
